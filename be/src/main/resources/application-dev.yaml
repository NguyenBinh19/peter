server:
  servlet:
    context-path: /demo
  port: 8080

spring:
  datasource:
    username: root
    password: root
    url: "jdbc:mysql://localhost:3306/peter"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true